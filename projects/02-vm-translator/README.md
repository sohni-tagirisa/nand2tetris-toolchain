# VM Translator

The first VM translator stage handles arithmetic, logical, comparison, push, and pop commands across the Hack VM memory segments.

```bash
python3 src/vm_translator.py examples/stacktest.vm
```
