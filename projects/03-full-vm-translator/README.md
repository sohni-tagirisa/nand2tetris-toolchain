# Full VM Translator

Extends the stack translator with program flow, function declarations, calls, returns, scoped labels, and conditional bootstrap generation. It accepts either one `.vm` file or a directory of VM files.

```bash
python3 src/vm_translator.py examples/FibonacciElement
```
