# Jack Compiler

A Jack-to-VM compiler built from a tokenizer, recursive compilation engine, scoped symbol table, and VM writer. It accepts a `.jack` file or a directory containing Jack source files.

```bash
python3 src/JackCompiler.py ../07-wordle/src
```
